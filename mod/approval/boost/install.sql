CREATE TABLE mod_approval_jobs (
  id int NOT NULL default '0',
  mod_title varchar(30) NOT NULL default '',
  mod_id int NOT NULL default '0',
  display text NOT NULL,
  PRIMARY KEY  (id)
  );
